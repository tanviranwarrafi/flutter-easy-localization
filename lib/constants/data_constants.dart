import 'package:flutter/material.dart';

/// Language Constants
const DEFAULT_LANGUAGE = 'en-US';
const FALLBACK_LANGUAGE = Locale('en', 'US');
const SUPPORTED_LOCALES = [Locale('en', 'US'), Locale('bn', 'BN')];
